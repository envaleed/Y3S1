<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.admin.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver
            android:name=".AlarmReceiver"
            android:process=":remote"/>
        <activity android:name=".MainActivity" />
        <activity android:name=".AddAssignment" />
        <activity
            android:name=".AddStudy"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".AddBudget" />
        <activity android:name=".AddPersonal" />
        <activity android:name=".ScheduleDate"/>
        <activity android:name=".progressSpinner"></activity>
    </application>

</manifest>